pair_scoring: |
  Context: {goal}
  
  Your task is to analyze and score multiple potential user pairs for conversation / introduction / collaboration given the above context and the following instruction:
  "{instruction}"

  You will generate a 'score' for each user pair that reflects how valuable this match would be based on the context and instruction above. A score of 1.0 indicates a perfect match, while 0.0 indicates no particular value. Don't be afraid to give scores lower than 0.5 if matches are not particularly valuable, there are many other candidates in the group.

  {user_profiles_xml_formatted}

  Return compact JSON with scores for all pairs:
  {json_format_hint}